# Coefficients Method for ds.glm Objects

Extract coefficients from a ds.glm object.

## Usage

``` r
# S3 method for class 'ds.glm'
coef(object, ...)
```

## Arguments

- object:

  A ds.glm object

- ...:

  Additional arguments (ignored)

## Value

Named numeric vector of coefficients
