% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.vertLMM.closed_form.R
\name{.ds_vertLMM_closed_form}
\alias{.ds_vertLMM_closed_form}
\title{LMM closed-form GLS driver}
\usage{
.ds_vertLMM_closed_form(
  conns,
  server_names,
  y_srv,
  peer_srv,
  data,
  y_var,
  x_ysrv,
  x_peer,
  lambda_i,
  transport_pks,
  session_id,
  verbose = FALSE,
  share_scale = 1,
  column_scales = NULL,
  standardize = FALSE,
  ring = "ring63"
)
}
\arguments{
\item{conns}{Named list of DS connections.}

\item{server_names}{Character vector of server names (names of \code{conns}).}

\item{y_srv, peer_srv}{Character. Outcome server name and peer server name.}

\item{data}{Aligned data-frame name on both servers.}

\item{y_var}{Response column (on \code{y_srv}).}

\item{x_ysrv, x_peer}{Character vectors of predictor names on each
server (after formula parsing).}

\item{lambda_i}{Numeric vector of length n_clusters.}

\item{transport_pks}{Named list of public keys per server.}

\item{session_id}{Active MPC session (cluster IDs stored within).}
}
\value{
list(coefficients, XtX, Xty, yty, n)
}
\description{
Computes the exact Laird-Ware generalised-least-squares
estimate for a random-intercept LMM without relying on
\code{ds.vertGLM}'s L-BFGS iteration. Directly assembles the
Gram matrix X'X and right-hand side X'y over the cluster-mean-
centred design via:
- local per-server block computations
- Beaver vecmul + FP-sum for cross-server entries
and solves \code{beta = solve(XtX, Xty)} client-side. Matches
\code{lme4::lmer} to FP precision (~1e-5 on small n).

Assumptions:
- Cluster IDs have been broadcast via
\code{dsvertLMMBroadcastClusterIDsDS} /
\code{dsvertLMMReceiveClusterIDsDS}.
- Transport keys are initialised in the session.
- Exactly K=2 servers (outcome + one peer).
}
\keyword{internal}
