% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.vertPCA.R
\name{ds.vertPCA}
\alias{ds.vertPCA}
\title{Principal components from a signed DP correlation artifact}
\usage{
ds.vertPCA(
  data_name = NULL,
  variables = NULL,
  n_components = NULL,
  analysis_id = NULL,
  cor_result = NULL,
  verbose = TRUE,
  datasources = NULL
)
}
\arguments{
\item{data_name}{Signed protected dataset name or reusable
\code{ds.vertFederation}. Ignored when \code{cor_result} is supplied.}

\item{variables}{Optional signed variable subset. Named owner lists are
supported when they agree with the Gaussian artifact.}

\item{n_components}{Number of components, or \code{NULL} for all.}

\item{analysis_id}{Mandatory signed Gaussian artifact id when
\code{cor_result} is not supplied.}

\item{cor_result}{An existing complete-case \code{ds.vertCor} result from the
same sticky Synopsis. Pairwise, arbitrary, and legacy \code{ds.cor} objects are
rejected.}

\item{verbose}{Logical progress flag.}

\item{datasources}{DataSHIELD connections.}
}
\value{
A \code{ds.pca} object with loadings, eigenvalues, explicit spectral
mechanism diagnostics and inherited DP provenance. Scores are unavailable.
}
\description{
Performs client-only eigen decomposition of the explicitly PSD-projected
complete-case matrix returned by \code{ds.vertCor}. It never accepts the
pairwise-complete correlation artifact, invokes the former Ring63/exact
correlation protocol, or returns individual component scores.
}
