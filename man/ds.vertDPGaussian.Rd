% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.vertDPGaussian.R
\name{ds.vertDPGaussian}
\alias{ds.vertDPGaussian}
\title{Bounded Gaussian regression from the sticky DP capsule}
\usage{
ds.vertDPGaussian(
  data_name,
  analysis_id,
  ridge = 0,
  server = NULL,
  datasources = NULL
)
}
\arguments{
\item{data_name}{Signed protected dataset name.}

\item{analysis_id}{Custodian-configured signed Gaussian artifact id.}

\item{ridge}{Explicit non-negative ridge penalty on normalized predictors.
The default zero preserves the unpenalized bounded-statistic estimand.}

\item{server}{Optional expected signed outcome-owner server name.}

\item{datasources}{DataSHIELD connections.}
}
\value{
A \code{ds.vertDPGaussian} object. It contains no classical standard
errors, p-values, individual fitted values, residuals, or scores.
}
\description{
Fits a descriptive Gaussian linear model from one signed sufficient-
statistic artifact. Outcome and predictors are clipped to custodian-owned
public bounds, collapsed once per admitted patient, normalized to \verb{[0,1]},
and restricted to a fixed complete-case estimand. The function performs one
sticky capsule retrieval and then only deterministic client post-processing.
}
