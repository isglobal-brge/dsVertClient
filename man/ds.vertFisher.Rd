% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.vertChisq.R
\name{ds.vertFisher}
\alias{ds.vertFisher}
\title{Federated Fisher exact test (same-server case)}
\usage{
ds.vertFisher(
  data_name,
  var1,
  var2,
  server = NULL,
  alternative = c("two.sided", "greater", "less"),
  conf.int = TRUE,
  conf.level = 0.95,
  datasources = NULL
)
}
\arguments{
\item{data_name, var1, var2, server, datasources}{Same semantics as in
\code{ds.vertChisq}.}

\item{alternative}{One of "two.sided" (default), "greater", "less"; only
consulted for 2x2 tables.}

\item{conf.int}{Logical. Return an odds-ratio confidence interval for 2x2.}

\item{conf.level}{Confidence level for 2x2 OR interval.}
}
\value{
An object of class \code{ds.vertFisher} (a
\code{htest}-compatible list plus table metadata).
}
\description{
Exact conditional p-value for a 2x2 (or small 2xK) contingency
table of two variables held at the same server. Uses the
\code{stats::fisher.test} implementation on the released count matrix
(itself already subject to datashield.privacyLevel small-cell
suppression), so this is a safe exact-inference complement to
ds.vertChisq when any expected cell count is below the chi-square
validity floor.
}
