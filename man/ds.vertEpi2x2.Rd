% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.vertEpi2x2.R
\name{ds.vertEpi2x2}
\alias{ds.vertEpi2x2}
\title{Epidemiological effect measures from a protected 2-by-2 table}
\usage{
ds.vertEpi2x2(
  x,
  exposed = 2L,
  event = 2L,
  level = 0.95,
  zero_correction = c("none", "if_zero", "always")
)
}
\arguments{
\item{x}{A 2-by-2 count matrix, or an object such as
\code{ds.vertChisq} containing an \code{observed} matrix.}

\item{exposed}{Row identifying the exposed group, as a name or index.
Defaults to the second row.}

\item{event}{Column identifying the event, as a name or index. Defaults to
the second column.}

\item{level}{Confidence level for Wald intervals on an ordinary aggregate,
or simultaneous DP-mechanism coverage when \code{x} is a DP-aware
\code{ds.vertChisq} result.}

\item{zero_correction}{One of \code{"none"} (default), \code{"if_zero"},
or \code{"always"}. The latter two explicitly request a 0.5
Haldane--Anscombe correction for ratio estimates and report that choice
in the result.}
}
\value{
For an ordinary aggregate, a \code{ds.vertEpi2x2} object with standard
epidemiological estimates and Wald intervals. For a DP-aware
\code{ds.vertChisq} result, a \code{ds.vertDPEpi2x2} object with simultaneous
mechanism-noise regions and no ordinary sampling interval.
}
\description{
Computes risks, risk difference, risk ratio, odds ratio, attributable
fractions among the exposed and in the population, and number needed to
benefit/harm from an existing aggregate 2-by-2 table. This function is
entirely client-side: it performs
no additional DataSHIELD query and therefore does not enlarge the disclosure
surface of the table-producing method.
}
\details{
A DP-aware \code{ds.vertChisq} object is routed through its validated
source capsule. Ordinary Wald formulae and continuity corrections are not
applied to DP-noised cells, and no additional server call is made.
}
