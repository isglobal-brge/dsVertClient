% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.vertNBFullRegTheta.R
\name{ds.vertNBFullRegTheta}
\alias{ds.vertNBFullRegTheta}
\title{Sticky-DP NB2 frontdoor}
\usage{
ds.vertNBFullRegTheta(
  formula,
  data = NULL,
  theta = NULL,
  joint = TRUE,
  theta_max_iter = 5L,
  theta_tol = 0.001,
  variant = "full_reg_nd",
  beta_max_iter = 2L,
  beta_tol = 1e-04,
  compute_covariance = TRUE,
  verbose = TRUE,
  datasources = NULL,
  ...,
  frequency = NULL,
  server = NULL,
  analysis_id = NULL
)
}
\arguments{
\item{formula, data, theta, joint, theta_max_iter, theta_tol, variant, beta_max_iter, beta_tol, compute_covariance, verbose, datasources}{Retained compatibility arguments. With \code{frequency}, only
\code{formula}, \code{data}, \code{verbose}, and \code{frequency} are
accepted; all legacy NB2 controls fail locally.}

\item{...}{Retained compatibility arguments; not evaluated.}

\item{frequency}{A released, validated \code{ds.vertDPFrequency} object for
a bounded count outcome. It enables only an intercept-only, no-inference
NB2 method-of-moments result.}

\item{server}{Required source-owner when the Frequency route resolves its
canonical signed Frequency artifact for the outcome.}

\item{analysis_id}{Custodian-configured signed NB2 likelihood-grid id for an
additive covariate formula.}
}
\value{
With \code{frequency} or \code{server}, an intercept-only coefficient-only
\code{ds.vertNBFullRegTheta} object. With \code{analysis_id}, an additive
finite-grid NB2 object with coefficients and \code{theta}, but no covariance
or sampling inference. Otherwise the function raises
\code{dsvert_route_unavailable} before DSI.
}
\description{
With a released, validated \code{ds.vertDPFrequency} object,
or an explicit \code{server} from which it can be read,
whose signed domain is bounded non-negative integer counts, this frontdoor
fits \code{y ~ 1} by deterministic NB2 method-of-moments post-processing.
With \code{analysis_id}, additive bare predictors select one candidate from
a signed sticky-DP finite \eqn{(beta, theta)} likelihood grid.
}
\details{
The Frequency route is intercept-only, never starts a new analysis,
and has no covariance or standard errors. The grid route requires a same-owner
signed artifact with bounded integer outcomes, finite beta and theta grids,
and public predictor bounds. Neither route runs iterative remote likelihood
calculations or returns sampling inference.
}
\seealso{
\code{\link{ds.vertMethodStatus}}
}
